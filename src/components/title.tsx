import "../App.css"; // Import your CSS file

const WordsAnimation: React.FC = () => {
  return (
    <div className="text">
      <p></p>
      {/* <ReactTextRotator content={content} time={5000} startDelay={2000} /> */}
    </div>
  );
};

export default WordsAnimation;
